//*******************************************************************************
// Course Number and Section:
// Course Semester:
// Your Name:
// --Only if he helped you with the assignment--
// Teammate Name:
// ---------------------------------------------
// Program Description:
//
//
//
//*******************************************************************************

//	01) Include your libraries here. Explain the libraries being used with comments.


//	DO NOT MODIFY THE LINE BELOW
#include <limits>

using namespace std;

int main()
{
	//	02) Below this comment, declare variable(s) named "numA" and "numB"
	//		that hold(s) double-precision real number(s).


	//	03) Below this comment, declare variable(s) named "resultA" that hold(s)
	//		double-percision real number(s) and initialize it to "0.0".


	//	04) Below this comment, declare variable(s) named "opSign" that hold(s)
	//		a character(s).

	
	//	05)	Below this comment, declare variable(s) named "errorCode" that hold(s)
	//		integer number(s) and assign it the value: 0.


	//	06) Below this comment, prompt the user:
	//			Please enter an expression (for example, 2 + 5): 


	//	07) Below this comment, get the value(s) from the keyboard and store them in
	//		the appropriate variable(s).


	//	08) Below this comment, check the operator and preform the appropriate 
	//		arithmetic as follows:
	//		Requirement(s) and algorithm(s)
	//			If operator is equivalent to addition, then
	//				01)	Calculate the expression: numA + numB and store in appropriate
	//					variable.
	//			Otherwise, if operator is equivalent to subtraction, then
	//				01)	Calculate the expression: numA - numB and store in appropriate
	//					variable.
	//			Otherwise, if operator is equivalent to multiplication, then
	//				01)	Calculate the expression: numA * numB and store in appropriate
	//					variable.
	//			Otherwise, if operator is equivalent to division, then
	//				If divisor is equivalent to zero, then
	//					01)	Assign integer error code one (1) to corresponding variable(s)
	//				Otherwise,
	//					01)	Calculate the expression: numA / numB and store in appropriate
	//						variable.
	//			otherwise,
	//				01)	Assign integer error code two (2) to corresponding variable(s)
	//		Note:	Use a nested decision-making structure for this part


	//	09) Below this comment, display the following messages based on the error code
	//		on the screen:
	//		Requirement(s) and algorithm(s)
	//			If error code is equivalent to zero (1), then
	//				01)	Display on the screen:
	//						Illegal operation: Division by zero...
	//			Otherwise, if error code is equivalent to one (2), then
	//				01)	Display on the screen:
	//						Invalid expression and/or operation...
	//			Otherwise,
	//				01)	Display on the screen:
	//						The result of [numA] [opSign] [numB] is [resultA].
	//		Note:	Use a switch structure for this part

	
	//	10)	Below this comment, display on the screen:
	//
	//
	//			Press ENTER key to continue...
	//		Note:	Pay attention to the new lines above the message


	//	DO NOT MODIFY THE LINE BELOW
	cin.get();
	//	DO NOT MODIFY THE LINE BELOW
	cin.ignore(numeric_limits<streamsize>::max(), '\n');

	// 11) Below this comment, terminate your program with integer 0.

}